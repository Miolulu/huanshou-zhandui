---
description: 幻兽战队项目改动后自动提交并推送到 GitHub Pages
globs: **/*
alwaysApply: true
---

# 自动部署（幻兽战队）

完成本项目的实质性改动后，**默认**执行 commit 并 push 到 `origin/main`，无需用户再次确认。

- 仓库：https://github.com/Miolulu/huanshou-zhandui
- 线上：https://miolulu.github.io/huanshou-zhandui/
- GitHub Pages 从 `main` 分支自动部署

## 提交规范

- 使用：`git -c user.name="Miolulu" -c user.email="283957080@users.noreply.github.com" commit`
- 仅提交与任务相关的文件；不提交 `.env`、密钥等敏感文件
- 用户明确说「不要推送」或「只改本地」时，跳过 push

## 例外

- 纯问答、代码审查、无文件改动：不提交
- 用户明确要求不 commit 时：遵守用户指令
